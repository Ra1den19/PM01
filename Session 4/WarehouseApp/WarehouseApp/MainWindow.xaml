<Window x:Class="WarehouseApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WarehouseApp"
        mc:Ignorable="d"
        Title="Warehouse App Login" Height="450" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#FF1E1E1E">
    <Grid>
        <Border Background="#FF252526" CornerRadius="10" Padding="20" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Warehouse App" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- TabControl для выбора способа входа -->
                <TabControl x:Name="LoginTabControl" Background="Transparent" BorderBrush="Transparent" Width="300" Margin="0,0,0,20">
                    <TabControl.Resources>
                        <Style TargetType="TabItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TabItem">
                                        <Border Name="Border" Background="Transparent" CornerRadius="5" Padding="10,5">
                                            <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#FF0E639C"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#FF1177BB"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="10,5"/>
                        </Style>
                    </TabControl.Resources>

                    <!-- Вкладка для входа по логину и паролю -->
                    <TabItem Header="Логин">
                        <StackPanel>
                            <TextBlock Text="Логин:" Foreground="White" FontSize="14" Margin="5,0,5,5"/>
                            <TextBox Text="{Binding Login}" Width="250" Margin="5" Padding="10" Background="#FF333333" Foreground="White" BorderBrush="#FF555555" BorderThickness="1" FontSize="14">
                                <TextBox.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="5"/>
                                    </Style>
                                </TextBox.Resources>
                            </TextBox>

                            <TextBlock Text="Пароль:" Foreground="White" FontSize="14" Margin="5,10,5,5"/>
                            <PasswordBox x:Name="PasswordBox" Width="250" Margin="5" Padding="10" Background="#FF333333" Foreground="White" BorderBrush="#FF555555" BorderThickness="1" FontSize="14">
                                <PasswordBox.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="5"/>
                                    </Style>
                                </PasswordBox.Resources>
                            </PasswordBox>
                        </StackPanel>
                    </TabItem>

                    <!-- Вкладка для входа по почте и паролю -->
                    <TabItem Header="Почта">
                        <StackPanel>
                            <TextBlock Text="Почта:" Foreground="White" FontSize="14" Margin="5,0,5,5"/>
                            <TextBox Text="{Binding Email}" Width="250" Margin="5" Padding="10" Background="#FF333333" Foreground="White" BorderBrush="#FF555555" BorderThickness="1" FontSize="14">
                                <TextBox.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="5"/>
                                    </Style>
                                </TextBox.Resources>
                            </TextBox>

                            <TextBlock Text="Пароль:" Foreground="White" FontSize="14" Margin="5,10,5,5"/>
                            <PasswordBox x:Name="EmailPasswordBox" Width="250" Margin="5" Padding="10" Background="#FF333333" Foreground="White" BorderBrush="#FF555555" BorderThickness="1" FontSize="14">
                                <PasswordBox.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="5"/>
                                    </Style>
                                </PasswordBox.Resources>
                            </PasswordBox>
                        </StackPanel>
                    </TabItem>
                </TabControl>

                <!-- Кнопка входа -->
                <Button Content="Войти" Command="{Binding LoginCommand}" Width="130" Height="30" Margin="5,20,5,5" Padding="10" Background="#FF0E639C" Foreground="White" FontSize="14" FontWeight="Bold" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF1177BB"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#FF0A4A6E"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>